# Zsh login profile configuration.

# Load common shell login profile configuration.
# Unable to statically follow since path is dynamic.
# shellcheck source=SCRIPTDIR/../sh/.profile
. "${HOME}"/.config/sh/.profile
